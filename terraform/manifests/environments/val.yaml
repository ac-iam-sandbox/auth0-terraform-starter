# val environment configuration.

tenant:
  auth0_domain: "REPLACE_WITH_val_DOMAIN"
  region: "na"

clients:
  marlo:
    callbacks:   ["https://val.marlo.example.com/callback"]
    logout_urls: ["https://val.marlo.example.com"]
    web_origins: ["https://val.marlo.example.com"]

vault_connections:
  auth0-m2m:
    domain:    "REPLACE_WITH_val_DOMAIN"
    client_id: "REPLACE_WITH_val_VAULT_CLIENT_ID"

action_modules:
  account-linking:
    MANAGEMENT_API_DOMAIN:    "REPLACE_WITH_val_DOMAIN"
    MANAGEMENT_API_CLIENT_ID: "REPLACE_WITH_val_MGMT_CLIENT_ID"

actions:
  enrich-signup-profile:
    API_BASE_URL: "REPLACE_WITH_val_API_URL"
