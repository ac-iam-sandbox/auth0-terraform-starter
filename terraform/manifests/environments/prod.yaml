# prod environment configuration.

tenant:
  auth0_domain: "REPLACE_WITH_prod_DOMAIN"
  region: "na"

clients:
  marlo:
    callbacks:   ["https://marlo.example.com/callback"]
    logout_urls: ["https://marlo.example.com"]
    web_origins: ["https://marlo.example.com"]

vault_connections:
  auth0-m2m:
    domain:    "REPLACE_WITH_prod_DOMAIN"
    client_id: "REPLACE_WITH_prod_VAULT_CLIENT_ID"

action_modules:
  account-linking:
    MANAGEMENT_API_DOMAIN:    "REPLACE_WITH_prod_DOMAIN"
    MANAGEMENT_API_CLIENT_ID: "REPLACE_WITH_prod_MGMT_CLIENT_ID"

actions:
  enrich-signup-profile:
    API_BASE_URL: "REPLACE_WITH_prod_API_URL"
