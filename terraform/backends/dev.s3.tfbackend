# Bucket name comes from TF_STATE_BUCKET in variable group na-dev-axon-cic.
# If using variable group value, override at init:
#   terraform init -backend-config="bucket=$(TF_STATE_BUCKET)" -backend-config=backends/dev.s3.tfbackend
key          = "auth0/dev/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
