# =============================================================================
# Environment: val (validation / pre-production)
# =============================================================================

locals {
  environment = "val"
}
